class ScrapingCase {
    constructor(realState, operation, propertyType) {
        this.realState = realState
        this.operation = operation
        this.propertyType = propertyType
    }
}

module.exports = ScrapingCase